# ESTADO al 18/09/2026 — handoff para retomar

**Repo público:** `C:\tmp\tasador-rag` → `https://github.com/ricardobing/tasador-rag`
**Repo privado (archivo, no se toca):** `C:\tmp\rag1`, rama `prep/publicacion`

> Empezá por acá. Qué se hizo hoy, qué está corriendo, qué falta, y cómo seguir.
> El plan completo es [doc 18](18-rag-propuesta.md); el estado por fase está en su §9;
> los resultados van a [informes/2026-09-18-rag-resultados.md](informes/2026-09-18-rag-resultados.md).

---

## 1. Qué se hizo (17-18/09)

1. **Publicación.** Árbol saneado desde el repo privado con `ops/generar_arbol_publico.py`
   (vive solo en el privado: contiene el mapeo real → anónimo). Sin captura de portales,
   sin contenido de terceros, sin datos del cliente. Rótulos neutros (`PORTAL_A/B`,
   `inmo-demo`); el mapeo de portales sale de `PORTALES=` en el `.env`. Gate:
   `ops/auditar_publicacion.py` (compara contra los VALORES del `.env`, nunca los imprime).
   Licencia PolyForm Noncommercial 1.0.0. Historial nuevo.
2. **Corpus demo.** `scripts/generar_corpus_demo.py`: 581 avisos sintéticos con las
   distribuciones del corpus real, descripciones redactadas por LLM a partir de hechos
   sorteados, golden set con **verdad por construcción** (`tests/golden/demo-extraccion.yaml`,
   60 avisos, 20 con descarte). `--reusar-csv` rehace el golden sin volver a pagar.
3. **RAG, fases 1-4 y 6 construidas** (doc 18 §9): `eval/retrieval.py` + `eval/juicios.py`
   (métricas con bootstrap apareado; pooling juzgado por el pipeline), `rag/`
   (chunking A/B/C con encabezado, embedder local fuera del event loop, indexador
   incremental, híbrido denso+léxico con RRF, reranker), migración `listing_chunks`,
   `POST /v1/reports/{id}/ask` con caja en la ficha, `scripts/eval_qa.py`.
4. **Decisiones medidas** (ADR-010 a 013 en doc 01 §4): `bge-m3` no lo sirve la librería;
   `e5-large` embebe a 0,4–0,8 pasajes/s en CPU (6-12 h para el corpus) → se mide con
   `MiniLM-L12` multilingüe (13/s, 128 tokens); el 28,1% de los avisos supera 512 tokens
   y casi ninguno entra en 128: el chunking es necesidad, no adorno. Columna `embedding`
   sin dimensión y sin HNSW (búsqueda exacta sobre el pool filtrado).
5. **Hallazgos que corrigieron el plan:** los juicios de informes pasados son relativos a
   un pool que ya no existe (`juzgados@25` = 0,27) → pooling; el tokenizador de
   `fastembed` trunca y contar con él dice "ninguno se pasa" → copia sin truncación;
   el chunker O(n²) tardó 323 s solo en contar → una tokenización por oración; el umbral
   denso del QA no separa con MiniLM (0,43-0,74 vs 0,32-0,51) → compuerta doble
   coseno + solapamiento léxico.

## 2. Lo que estaba corriendo al cortar (18/09 ~22:35)

| Proceso | Log | Estado al cortar | Reanudar |
|---|---|---|---|
| Indexado chunker **C** (MiniLM) | `C:\tmp\index-C.log` | ✅ terminado: 8.514 avisos → **83.063 chunks** (9,76/aviso, p50 92 tokens, máx 120) en 4.020 s | incremental: sin cambios escribe 0 |
| Indexado chunker **A** | `C:\tmp\index-A.log` | ✅ terminado: 8.514 chunks (1/aviso) en 528 s | ídem |
| Juicios del pool, sistema A | `C:\tmp\juzgar-A.log` · `data/eval/retrieval/*.json` | 106 de 113 consultas | reanudable: saltea lo ya juzgado |
| Juicios del pool **A-E** (tres procesos en paralelo) | `C:\tmp\juzgar-pool-{1,2,3}.log` | consultas 0-33, 34-66, 67-99; falta 100-112 | `eval_retrieval.py --sistemas A,B,C,D,E --juzgar --avisos 60 --desde 100 --limite 13` |

Los tres necesitan `DATABASE_URL` apuntando al host (`127.0.0.1:5433`), `LITELLM_BASE_URL`
y el stack `postgres`, `redis`, `litellm` arriba. El `.env` local ya tiene
`COMPOSE_PROJECT_NAME=tasador` para reusar los volúmenes de la base real.

### 2.1 El primer run del CI (18/09, tras el push)

Falló en dos jobs, los dos reproducibles en local:

- **Secretos filtrados (gitleaks):** dos falsos positivos —el placeholder de
  `SECRET_KEY` en `.env.example` y el `Bearer tsk_live_...` del manual—. Arreglado con
  `.gitleaks.toml` (allowlist por FORMA del placeholder, no por archivo); verificado con
  la misma imagen en Docker: `no leaks found`.
- **Tests:** reproducido con una base vacía `tasador_test` y el umbral de cobertura del
  CI (`--cov-fail-under=57`); ver §3.

### 2.2 Sesión del 18/09 (continuación)

- **CI en verde completo** (run 35311610266, commit 235e0a7): lint, tipos, tests,
  gitleaks, migraciones desde cero y las tres imágenes. Antes cayó tres veces en
  «Imágenes» por `trivy`: Next 15.1.6 con CVE-2025-29927 → `next@15.5`; `npm` y
  `postcss` en la imagen web; `pcre2` y `supercronic` en api/worker (abajo).
- **Crítico sobre `deepseek-v4-flash`** (decisión de costos). `deepseek-v4.1-flash`
  falló dos veces la sonda estructurada («Palermo» por «Belgrano»).
- **Guardia de memoria** (`eval/memoria.py`, `--memoria-libre`): seis procesos
  huérfanos dejaron 0,1 GB de 32 al cortarse la sesión anterior. Correr a lo sumo
  dos evals en paralelo.
- **Set de consultas fijado** en `data/eval/retrieval/_consultas_avisos.json` (60
  avisos): antes cambiaba entre corridas porque el eval crea features.
- **«Preguntale al informe» probado de punta a punta** contra la API en el
  contenedor y con Playwright (1 passed). QA sin caché: rechazo 0,96 · citas 0,89 ·
  cifras 0,94.
- **Ablación, primera mitad (57 consultas)**: D +0,125 · B +0,101 · E +0,037 ·
  C +0,021 en nDCG@25 sobre A, todos fuera del intervalo salvo C (roza el cero).
  Logs: `C:\tmp\juzgar-pool-5.log` (0-56), `juzgar-pool-6.log` y `-7.log` (57-112).
- **Juicios contaminados y rehechos.** El reinicio del gateway por el cambio de
  crítico (01:33-01:41) cayó en medio del juicio de la segunda mitad: 16 consultas
  quedaron juzgadas con «el juez no respondió» (el nodo 6 degrada: sin juez
  sobreviven todos, y la fracción de grado 2 subió de 0,80 a 0,86). Se apartaron
  y se volvieron a juzgar con el gateway estable (`juzgar-pool-7.log`). Lección:
  no reiniciar el gateway con un juicio en curso, o dejar que el guardián lo
  detecte —hoy no lo detecta, y es una fila pendiente.
- **Pooling incremental.** `juzgar_pool` recibe los juicios guardados y juzga
  SOLO los candidatos que ningún sistema había traído: agregar F (rerank) cuesta
  sus candidatos nuevos y no el pool entero, y los juicios de A-E no cambian entre
  corridas (`tests/test_eval_juicios.py`).
- **Playwright completo tras `next@15.5`**: 50 passed · 7 skipped (5 min). Hizo
  falta `npx playwright install chromium` (la actualización de deps cambió la
  versión del navegador y la suite daba 53 failed por navegador ausente).
- **La RAM, segunda vuelta.** Con el guardián puesto, los evals quedaron esperando
  igual: el VM de Docker (`vmmemWSL`) llegó a 12 GB tras los builds y no los devuelve,
  y el proceso del reranker llegó a 5,6 GB porque `TextCrossEncoder.rerank` mete los
  60 pares de 512 tokens en un solo lote (ONNX reserva la activación de todo el lote).
  Dos arreglos: `%USERPROFILE%\.wslconfig` con `memory=14GB`, `swap=4GB` y
  `autoMemoryReclaim=gradual` (requiere `wsl --shutdown` y reiniciar Docker Desktop;
  hecho), y `Reranker.BATCH = 8`.
- **F medía E.** `reordenar` tiene un tope de 15 s (decisión de latencia del producto) y
  degrada al orden del híbrido; en CPU 60 pares tardan 17-35 s, así que la fila F se
  estaba juzgando con el ranking de E. Además, `asyncio.wait_for` no cancela el hilo
  del `to_thread`: cada consulta dejaba un rerank corriendo de fondo y el proceso
  subió a 4,7 GB (el reranker solo mide 1,4 GB cargado, 1,6 GB tras 60 pares). En el
  eval el tope es 300 s y degradar es error.
- **Segunda contaminación, misma causa, otra forma.** Tras reiniciar Docker Desktop
  (por el `.wslconfig`), el proxy de puertos del host perdió primero `5433` y después
  `4000` ("server closed the connection" / `Connection error`) con los contenedores
  sanos; `docker compose restart <servicio>` lo recompone. La corrida de F juzgó 41
  consultas con el juez caído: como el nodo 6 degrada, los candidatos nuevos quedaron
  en grado 2. Se repararon quitando de cada archivo los últimos N ids del pool (los
  que F agregó; copia en el scratchpad) y ahora `curate` cuenta `lotes_sin_juez` y
  `eval/juicios.juzgar` **corta sin guardar** si es > 0.
- **Demo desde cero, hecha de verdad** (clon limpio en `C:\tmp\tasador-demo`, `.env`
  desde la plantilla, proyecto `tasador-demo` en puertos 5434/6381/4001/8001/3001 al lado
  del stack real). Lo que el README prometía y no pasaba: (1) `seed.py` no sembraba los
  barrios —solo `load_badata.py` lo hacía—, el paso de centroides decía
  `geocodificados=0` con cara de éxito, la ingesta dejaba los 581 avisos sin barrio y el
  informe moría con `BARRIO_NO_RESUELTO`; ahora `seed.py` los siembra (paso 2/3).
  (2) Los `uv run` del README no pueden resolver `postgres:5432` desde el host: los
  scripts van con `docker compose … run --rm api python …` y `--carpeta /data/raw/demo`
  (desde Git Bash hace falta `MSYS_NO_PATHCONV=1`). (3) El primer informe salió
  `SUCCEEDED` (USD 226-339 mil, ALTA, 45/60, USD 0,067, 877 s) pero **sin narrativa**: el
  crítico en `deepseek-v4-flash` rechazó tres veces, dos de ellas por "confianza ALTA no
  sostenida" cuando el motor calculó ALTA, y una por el rango de cierre condicional que
  el prompt ya declaraba resuelto. `prompts/critic/v2.jinja` deja explícito que el
  nivel de confianza lo calcula el motor y no se discute, y que el rango de cierre y
  los precios de oferta no son hallazgo. (4) `POST …/ask` desde afuera: respuesta con
  citas `[N]` `[V]` en 87 s la primera vez (embebe el informe); la pregunta sin
  respuesta se rechaza en 80 ms sin modelo. El `curl` de Git Bash manda el JSON en
  cp1252 y la API devuelve 400: probar con Python o PowerShell.
- **El guardián al 20% frena los evals en reposo.** Con el stack real levantado (VM
  de Docker ~7,5 GB) más navegador y editor, la máquina queda en 19-20% libre sin
  correr nada; el guardián espera 600 s por consulta y F avanzaba una consulta cada
  10 minutos. F y H corren con `--memoria-libre 0.12` (3,8 GB libres mínimos, que sigue
  siendo una barrera contra el agotamiento); el 20% queda como default para no dejar
  la máquina inusable cuando hay más de un proceso.
- **Primer informe real con el léxico encendido** (Gorriti 5000, 3 amb, 70 m²): nodo 2 en
  397 ms (pool 252, 249 puntuados), 57/60 sobreviven a la curaduría (48-50 con recencia
  en agosto), ALTA, 51 usados. Pero **el crítico se cortó en 4.096 tokens de salida**
  («output is incomplete due to max_tokens») y el informe salió sin narrativa: el
  modelo abierto escribe más que Sonnet. `max_tokens: 8192` para `critic` y el prompt
  v2 pide a lo sumo seis hallazgos breves. A/B del mismo sujeto, mismo día, léxico
  contra recencia: ver `C:\tmp\real-ab.log` y el punto siguiente.
- **A/B del mismo sujeto, mismo día** (Gorriti 5000, 3 amb, 70 m²): léxico → ALTA,
  57/60 sobreviven a la curaduría, 51 usados, mediana USD 207.060; recencia → MEDIA,
  58/60, 47 usados, USD 229.250. El pool léxico trae comparables que el motor usa más
  y con más confianza; la diferencia de valor no se atribuye sin más casos (el
  backtest de 600 × 3 semillas es la medida: −2,2 pp de MdAPE).
- **El crítico, tercera vuelta: sin razonamiento extendido.** Con 8.192 tokens el
  modelo abierto tardaba 95-110 s por crítica y el tercer intento se cortaba a los
  180 s (timeout del cliente HTTP, no del nodo ni del gateway). `provider_params:
  reasoning.enabled: false` en el nodo (antes el crítico no pasaba `provider_params`
  al cliente): crítica en 26 s y 5 s, informe real `SUCCEEDED` con narrativa de 3.448
  caracteres en 69 s (un rechazo legítimo por conteo de descartes, aprobado al segundo
  intento). Timeouts alineados: nodo 240 s, gateway 240 s, cliente 300 s.
- **Mapa del proyecto con Archify** (`docs/mapa/`): siete diagramas interactivos
  (arquitectura con evidencia enlazada al commit, dos workflows del pipeline, dos
  secuencias, flujo de datos del RAG y su medición, ciclo de vida del informe) con sus
  JSON versionados y un README que explica cómo funciona, qué se construyó y cómo
  contarlo. Los siete pasan `validate --quality showcase` (9 chequeos de artefacto) y
  `visual-check` (contención a 1440×900, 1600×1000, 1920×1080 y 2048×1320 en claro y
  oscuro); las tarjetas se recortaron a un ítem para que entren en pantalla.
- **Métrica `descartados@30`** en el eval de recuperación: fracción del top-30 juzgado
  con grado < 2. Es el criterio (b) de doc 18 §4.4 leído de los juicios del pooling.
- **Backtest con selección por recuperador** (`run_backtest.py --seleccion A|E --seeds`):
  el criterio (c) de doc 18 §4.4. Antes el backtest elegía comparables por superficie
  sin pasar por el nodo 2, así que `semantic.enabled` no podía cambiarle el MdAPE.
- **Imágenes `api`/`worker`: `apt-get upgrade`** (tres HIGH en `pcre2` de la base de
  Debian) y **supercronic v0.2.49** (el v0.2.33 traía la stdlib de Go 1.24 con 21 HIGH
  + 1 CRITICAL en `net/http`).
- **Imagen web sin `npm` en el runtime y `postcss` fijado por `overrides`**
  (`^8.5.12`; venía 8.4.31 vía Next): lo que trivy marcaba HIGH en «Imágenes».

## 3. Dónde quedó (19/09, madrugada) y lo que falta

**Hecho y verificado:** tabla de ablación A–H sobre 113 consultas con juicios completos;
backtest con selección por recuperador (600 casos × 3 semillas, superficie/A/D/E/H);
decisión tomada y encendida (`semantic.enabled: true`, `modo: lexico`; ADR-012, doc 04,
informe §3); QA «Preguntale al informe» medido y probado desde una base vacía; demo
completa desde un clon limpio (seed → ingesta → usuario → informe → pregunta); CI en
verde con las tres imágenes limpias; crítico v2 sobre modelo abierto.

Lo que falta, en orden de valor:

1. **Un informe real con el flag encendido, comparado con el mismo sujeto apagado**:
   comparables usados, confianza, descartes, tiempo. Está corriendo/hecho al cierre
   (ver §2.2, último punto) y es lo primero que hay que mirar al retomar.
2. **e5-large sobre un subconjunto** (`embed_corpus.py --modelo intfloat/multilingual-e5-large
   --barrio Belgrano`): la única fila que podría dar vuelta el "léxico gana" es un
   modelo de embeddings mejor que el que entra en una tarde de CPU. 0,4-0,8 pasajes/s.
3. **Juez LLM de fidelidad** para el QA: hoy solo se verifican cifras y citas; que la
   prosa no afirme lo que el fragmento no dice sigue sin medirse (informe §4).
4. **Anotación humana dirigida** del top-10 no juzgado (doc 18 §4.1, defensa 2): los
   juicios son del nodo 6, un anotador débil marcado como tal. 30 consultas × 10.
5. **Tope de latencia del reranker en producción** si algún día se enciende F: 15 s
   degrada siempre en CPU; o 60 s, o GPU, o no encenderlo (hoy: no encenderlo).
6. ~~Playwright en el CI~~ → hecho el 19/09: job `Pantallas` contra un stack de Compose
   con el corpus demo (sin clave de modelo: los tests que piden un informe `SUCCEEDED`
   se saltean allá). Y el CI entero corre en local con `make ci` / `.\make.ps1 ci`
   (`ops/ci-local.sh`): pushear es confirmar. Guía: `docs/guias/probar-la-app.md`.
7. **`.wslconfig`**: quedó en `memory=10GB` + `autoMemoryReclaim=dropcache` para que
   los evals convivan con Docker Desktop; si molesta para otra cosa, subirlo.

## 4. Cómo verificar que nada se rompió

```powershell
$env:DATABASE_URL="postgresql+psycopg://tasador:<PASS>@127.0.0.1:5433/tasador"
uv run ruff check src tests scripts ops migrations
uv run mypy --strict src
uv run alembic check
uv run pytest -m "not live" -q
uv run python ops/auditar_publicacion.py --historial
```

Última corrida completa antes del commit: ruff y mypy limpios, `alembic check` limpio,
pytest en verde salvo tres fallas que se arreglaron en el mismo commit (aislamiento
por `org_id` en `ask.py`, `bundle.lock.json` desactualizado por el prompt `qa/v1`, y el
test del embedder que asumía vectores normalizados).

## 5. Números medidos hoy (para el informe de resultados)

```
tokens por aviso (MiniLM/XLM-R, sin truncar, n=8.514)   p50 374 · p90 710 · p99 1.176 · máx 2.400
                                                        > 512: 28,1% · > 320: 60,4%
chunker C con e5 (objetivo 320)                         2,06 chunks/aviso · p50 276 · máx 480
chunker C con MiniLM (objetivo 100)                     ~10,6 chunks/aviso (600 avisos → 6.545)
embeddings en CPU (12 núcleos)                          e5-large 0,4-0,8/s · MiniLM 13/s
rerankers en CPU                                        bge-base 1,7 pares/s · jina-v2 1,8 pares/s
juicios A (pool = su propio top-60)                     nDCG@25 0,64 · recall@60 1,0 (trivial)
QA con umbral denso solo (e5-style 0,80, MiniLM)        rechazo 0,31 · citas 0,00: no separa
QA cosenos MiniLM                                       con respuesta 0,43-0,74 · sin 0,32-0,51
```
